"""Дополнительные power‑up функции для Infinite Tic‑Tac‑Toe.

В этом файле находятся вспомогательные power‑up, которые не входят в
основной набор, но могут быть использованы в дальнейшем развитии игры.
"""

from __future__ import annotations

from src.powerups import Board


def extra_turn(board: Board) -> bool:
    """Power‑up «дополнительный ход».

    Функция не меняет состояние доски, а просто возвращает ``True``.
    Вызывающий код может использовать возвращаемое значение, чтобы
    пропустить ход компьютера и дать игроку возможность сделать ещё один ход.
    """
    # Текущая реализация всегда предоставляет дополнительный ход.
    return True

