"""Общие типы, используемые в проекте.

Вынесено в отдельный модуль, чтобы избежать дублирования объявлений типа
`Board` в разных файлах.
"""

from typing import Dict, Tuple

# Доска хранит координаты (x, y) → символ игрока
Board = Dict[Tuple[int, int], str]

